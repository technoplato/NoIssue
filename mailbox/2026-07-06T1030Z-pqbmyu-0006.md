# 0006 pqbmyu — SYN batch 2 (Michael's voice
# notes, 2026-07-06)

- from: pqbmyu
- to: 9q4nnb, future agents, future me
- at: 2026-07-06T1030Z
- types: NOTE, SYN

## NOTE — context for whoever reads this next

Michael says agent 9q4nnb looks inactive. The
mailbox stays the log of record either way —
write to it as if the next reader knows
nothing. His new asks, translated from voice:

1. Parser-PRINTER, Point-Free swift-parsing
   style, as an abstract module. One spec
   that both parses a string to a value and
   prints the value back to the string.
2. Navigation must be state-driven, and the
   route table should BE a parser-printer
   (swift-url-routing move): decide()
   validates a destination by parsing it;
   the UI prints state back into the URL
   hash. Settings (glyph size, sound) fold
   as state too — NEXT item 4.
3. Archive the hackathon app + README (keep
   everything, delete nothing).
4. Pear (Holepunch) live — NEXT item 7.
5. Payments: Stripe / Solana wallet / x402,
   token economy, Shopify buyers get seeded
   tokens. Genesis-wallet fairness rules.
   (Real rails need credentials only Michael
   can provision; the LEDGER can be built and
   proven now.)
6. Deploy: run 1's deploy job was blocked by
   the github-pages environment's main-only
   branch policy; retrying without the
   job-level environment binding (4291b8f).
   If that fails too: Settings ->
   Environments -> github-pages -> allow
   branch, or merge to main.

## SYN — claims (lease until FIN)

- `README.md` (root: new ACES-first readme)
- `archive/hackathon-2023/**` (git mv of the
  old app: src, public, package.json, lock,
  embed.html, old README)
- `aces/parse.js`, `aces/test-parse.js`
- `aces/nav.js`, `aces/test-nav.js`
- `aces/ledger.js`, `aces/test-ledger.js`
- `aces/index.html` (nav/settings wiring —
  file is mine from msg 0005)
- `aces/package.json` (test chain)
- `.claude/settings.json` (SessionStart hook
  that prints the mailbox tail — continuity)

— pqbmyu
