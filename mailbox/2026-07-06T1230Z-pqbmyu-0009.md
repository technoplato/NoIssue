# 0009 pqbmyu — FIN batch 3

- from: pqbmyu
- at: 2026-07-06T1230Z
- types: FIN, NOTE

## FIN — batch 3 shipped (mailbox 0008 claims)

All done, pushed to
`claude/aces-engine-architecture-pqbmyu`
(commits 2099f27 + lockfile). NOT on main.

- **Docs**: sharpened effect/dependency language
  in SUMMARY.md + AGENTS.md (storage/network/
  screen/payments are SIDE-EFFECTS performed by
  injected DEPENDENCIES — dependencies cause the
  effects; react only names them). Added a TOC to
  each. Corrected deploy status (arcade NOT live,
  kept off main).
- **Pear live** (`pear.live.js` + test): REAL
  hypercore replication behind the {publish,
  subscribe} contract. Two nodes, a fact crosses
  by genuine replication — 4/4. Swarm/DHT
  discovery is real code but needs a UDP host
  (this sandbox blocks UDP). Deps are OPTIONAL;
  core suite stays dependency-free.
- **Stripe payments** (`pay.js` + test +
  pay.stripe.md): a charge is a side-effect behind
  a `pay` dependency; a verified webhook re-enters
  the ledger as RecordPurchase → tokens minted
  ($5→50,000). Forged signatures rejected, double-
  spends refused twice over. 10/10 vs a scripted
  double. Go-live needs a Stripe key + server
  (documented; no key in repo).

Suite: 119 core + 4 pear = 123 checks green.

## NOTE — main was reverted earlier

Reminder from msg 0008: `main` is back at its
pre-ACES commit (f5f5c71). Do NOT merge to main
without Michael's explicit say-so. All work lives
on the feature branch.

## NOTE — still not live (for the next builder)

- InstantDB: adapter ready, not wired live.
- Pear discovery: needs a UDP host to find peers.
- Payments: needs a live Stripe key + a small
  server + webhook endpoint.
- Solana: designed (ledger.solana.md), not
  written; x402 docs still to verify.

No open leases from me.

— pqbmyu
