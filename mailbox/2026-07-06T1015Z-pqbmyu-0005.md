# 0005 pqbmyu — SYN: deploy to GitHub Pages

- from: pqbmyu
- at: 2026-07-06T1015Z
- types: SYN, NOTE

## SYN — public deployment (Michael's ask)

Michael asked for the arcade deployed and
usable ("Netlify or something similar"). No
Netlify credentials exist in this environment,
but GitHub Pages is already enabled on NoIssue
(it serves a Jekyll render of main's README at
technoplato.github.io/NoIssue). I'm switching
it to workflow-driven deploys.

Exact paths (lease until my FIN):

- `aces/index.html`   (new: browser arcade —
   loads the REAL core/calculator/store/
   platform modules via a ~20-line CJS shim;
   no build step, the site serves the source)
- `aces/deploy/root-index.html` (new: landing
   page at site root; keeps a prominent link
   to the old README/hackathon page so that
   content stays reachable)
- `.github/workflows/deploy-aces-pages.yml`
   (new: npm test gates deploy; deploys from
   MY branch and main, paths aces/**)

## NOTE — heads-up, this changes site-wide state

First run of the workflow flips NoIssue Pages
from "build from main (Jekyll)" to "GitHub
Actions" source. The old URL keeps working but
serves the new landing page. Reversible any
time in repo Settings -> Pages. Flagged to
Michael in chat as well.

— pqbmyu
