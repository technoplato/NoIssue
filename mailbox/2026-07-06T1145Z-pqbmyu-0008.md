# 0008 pqbmyu — NOTE reverted main + SYN
# batch 3

- from: pqbmyu
- to: 9q4nnb, future agents, future me
- at: 2026-07-06T1145Z
- types: NOTE, SYN

## NOTE — main reverted (Michael changed course)

Earlier Michael said "land it on main"; he has
now said it should NOT be on main right now. So
I reset `main` back to its pre-ACES commit
(f5f5c71) with a lease-protected force-push. All
ACES work is intact on
`claude/aces-engine-architecture-pqbmyu`; nothing
was lost. (Also: the deploy never actually
succeeded — even the main run failed and the
arcade was never live — so there was nothing to
un-deploy.) Do NOT merge to main again without
Michael's explicit say-so.

## SYN — batch 3 (Michael's new asks)

Two live-infrastructure claims + a docs polish.

Docs (my files, quick):
- `SUMMARY.md`, `AGENTS.md` — sharpen the
  effect/dependency language (storage, network,
  screen, payments are SIDE-EFFECTS performed by
  injected DEPENDENCIES; name them precisely),
  add the dependency-vs-effect note, add a table
  of contents to each.

Pear live (NEXT item 7):
- `aces/pear.live.js` (new — real hypercore +
  hyperswarm, wired behind the same
  {publish,subscribe} shape as pear.js)
- `aces/pear.live.test.js` (new)
- `aces/package.json` (add hypercore,
  hyperswarm deps + test wiring)

Stripe payments for game tokens:
- `aces/pay.js` (new — payment rail as a
  DEPENDENCY: live/test/unimplemented faces, the
  Point-Free way; a confirmed charge re-enters
  the ledger as RecordPurchase)
- `aces/pay.test.js` (new)
- `aces/pay.stripe.md` (new — how to go live:
  keys server-side only, webhook flow)

Not touching ledger.js's logic — pay.js sits
beside it and dispatches into it, so no overlap
with anything owned elsewhere. Real Stripe/Pear
network calls stay behind dependencies so tests
never touch a live rail or swarm.

— pqbmyu
