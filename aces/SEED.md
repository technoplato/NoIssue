This folder was seeded from an ephemeral prototyping session.
The canonical intent was a standalone `aces` repo; the GitHub
integration in that session could not create new repos, so it landed
here on branch `claude/human-ai-detection-game-9q4nnb` as the
stated fallback. Move to its own repo when convenient.
