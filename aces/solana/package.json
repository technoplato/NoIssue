{
  "name": "aces-ledger-solana",
  "version": "0.1.0",
  "private": true,
  "description": "ACES token ledger — Anchor program + localnet test. Mirrors aces/ledger.js on chain.",
  "scripts": {
    "test": "anchor test",
    "build": "anchor build",
    "keys": "anchor keys sync"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.30.1"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.0"
  }
}
